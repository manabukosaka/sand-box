interface:
  display_name: "DB Tuner"
  short_description: "Tune DuckDB storage and queries."
  default_prompt: "Use the db-tuner skill to review Mini Datadog DuckDB ingestion, schema, query plans, retention, compaction, and growth risks."
