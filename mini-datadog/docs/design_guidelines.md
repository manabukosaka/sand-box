# UI/UX Design Guidelines

本ドキュメントは、プロジェクトにおけるUI/UXの品質を世界最高水準に保つための設計指針およびデザインシステムの枠組みを定義するものです。

## 1. デザイン原則 (Design Principles)
- **一貫性 (Consistency):** アプリケーション全体で統一された視覚言語（カラーパレット、タイポグラフィ、スペーシング）を使用する。
- **明確さ (Clarity):** ユーザーが迷わずに操作できるよう、直感的なナビゲーションと明確な情報階層を構築する。
- **アクセシビリティ (Accessibility):** すべてのユーザーが快適に利用できるよう、コントラスト比やキーボードナビゲーションに配慮する。

## 2. デザインシステム (Design System)
本プロジェクトでは以下の技術スタックとツールを用いてデザインシステムを構築・運用する。
- **CSS Framework:** Tailwind CSS
- **UI Components:** shadcn/ui
- **Icons:** (プロジェクトの標準アイコンセットを指定)

### 2.1 カラースキーム
（後日詳細を定義）
- Primary Color: 
- Secondary Color: 
- Accent Color: 
- Background / Surface:
- Text:

### 2.2 タイポグラフィ
（後日詳細を定義）
- Font Family: 
- Font Sizes:
- Font Weights:

### 2.3 スペーシングとレイアウト
- Tailwindの標準スペーシングスケールに準拠し、一貫した余白とレイアウトを設ける。

## 3. インタラクション (Interactions)
- マイクロインタラクションを活用し、ユーザーの操作に対するフィードバック（ホバー効果、ローディング状態、成功/エラーのフィードバックなど）を適切に提供する。

## 3.1 高密度オブザーバビリティ UI
- SRE / 開発者が短時間で判断できるよう、KPI、分布、時系列、ログテーブルを同一画面に密度高く配置する。
- カードは個別の情報単位に限定し、ページ全体を card-in-card にしない。
- severity color は ERROR / WARN / INFO / DEBUG の判別にのみ強く使い、装飾目的の単一色支配を避ける。
- chart は初期段階では SVG / CSS で実装し、依存追加は繰り返し必要性が確認できてから判断する。

## 3.2 Frontend-only preview の扱い
- Alerts と Settings のように backend 永続化が未実装の機能は、画面上で `Preview` または `Frontend-only` と明示する。
- preview UI は将来の情報設計検証を目的とし、保存済み・有効化済みと誤解される表現を避ける。
- backend API が追加された時に置き換えやすいよう、form state と表示 state は画面内に閉じる。

## 4. デザイン・レビュープロセス (Design Review Process)
実装が完了したPRは、マージ前に必ず以下のプロセスを経ること：
1. **セルフチェック:** 開発者は本ガイドラインおよびデザイン要件に準拠しているか確認する。
2. **デザイン・レビュー:** UI/UXデザイナーとPMが、視覚的な一貫性、インタラクションの滑らかさ、ユーザー体験の質をレビューする。
3. **承認:** レビューでの指摘事項を修正し、デザイン品質の承認を得てからマージする。
